name: FE Blog Docker

on:
  push:
    branches:
      - main
    paths:
      - 'apps/frontend/blog/**'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build_and_publish:
    runs-on: ubuntu-latest
    environment: Configure Docker
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push blog image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./apps/frontend/blog/Dockerfile
          push: true
          tags: ${{ secrets.DOCKERHUB_USERNAME }}/fe-blog:latest

      - name: Trigger Dokploy deployment
        run: |
          # Replace with actual webhook URL
          echo "Please configure the Dokploy webhook for blog service"
          # curl https://dokploy.verifio.email/api/deploy/YOUR_WEBHOOK_ID
