# Node Environment
NODE_ENV=development

# Service Configuration
PORT=8001
BASE_URL=https://local.verifio.email

# Database (PostgreSQL)
# Required for storing verification results and bulk jobs
PG_URL=postgresql://postgres:postgres@localhost:5432/verifio

# Redis Configuration
# Required for caching and session management
REDIS_URL=redis://:verifio123@localhost:6379

# Email Verification Settings
MAX_BULK_EMAILS=10000
DEFAULT_TIMEOUT=5000
